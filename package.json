{
  "name": "pickup-games",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "npx live-server --port=3000 --no-browser",
    "dev": "npx live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
